import { addNewCommentActions, addNewCommentSlice } from "./addNewCommentSlice";

describe("addNewCommentSlice", () => {
    test("should work with empty state", () => {
        expect(true).toEqual(true);
    });
});

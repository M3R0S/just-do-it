// import { articleDetailsActions, articleDetailsSlice } from "./articleDetailsSlice";

describe("articleDetailsSlice", () => {
    test("should work with empty state", () => {
        expect(true).toEqual(true);
    });
});

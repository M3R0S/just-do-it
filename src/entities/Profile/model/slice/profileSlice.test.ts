import { profileActions, profileSlice } from "./profileSlice";

describe("profileSlice", () => {
    test("should work with empty state", () => {
        // expect(
        //     profileSlice.reducer(undefined, profileActions.))
        // ).toEqual({});
        expect(true).toBe(true);
    });
});
